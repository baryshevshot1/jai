# A2 — Backend / Rust (часть 1: отказы, наблюдаемость, зависимости)

Дата: 2026-08-04, сессия A2a. Ревизия: `3ae5bbe`. Часть 2 (жизненный цикл процессов, гонки, файловая система, async-дисциплина) — следующая сессия, скоуп в 00-progress.md. Уровни доказательства: E0 чтение, E1 инструмент, E2 запуск здесь, E3 целевая среда.

## 1. Канал выпуска: расследование по пункту 1 заказчика (E1)

**Таймлайн (GitHub API + git-история + документы):**

| Дата (UTC) | Событие | Доказательство |
|---|---|---|
| 10.07 23:45 | release.yml, прогон №1 — **failure** (сломана Windows-сборка; чинилась PR #3 «сигнатура GetDesc в windows 0.58») | API: run 29131113607 |
| 11.07 00:02 | №2 — success → app-v0.1.0 (остался черновиком) | API: run 29131724956 |
| 11.07 05:47 | ci.yml создан («проверки на каждый PR») | коммит e3974f0 |
| 11.07 12:26 | №3 — success → app-v0.2.0 (опубликован) | API: run 29152616808 |
| **12.07 01:24–01:36** | **№4 — success → app-v0.3.0. Последний прогон release.yml вообще** | API: run 29175202568 |
| 12.07–02.08 | Активная разработка (PR #25–34): тег 0.4.0 не ставился — версия готовилась | git log |
| **03.08** | **Блокировка биллинга зафиксирована** («С 3 августа 2026 прогоны падают за 3–10 секунд» — docs/RELEASE.md:83); тем же днём ci.yml/audit.yml переведены на workflow_dispatch (e9daba3, 04:12) и подготовлен выпуск 0.4.0 (97d71d8) — который не состоялся | docs/RELEASE.md:81-94; git log -S |

**Вердикт по гипотезе заказчика: подтверждена с одним уточнением.** Релизы действительно собирал только Actions по тегу, и локальной замены сборщику Windows/Linux не сделали (Tauri не кросс-компилирует). Уточнение: после 12.07 нет ни одного прогона release.yml — даже упавшего. Выпуск встал не потому, что прогоны по тегу начали падать, а потому что тег 0.4.0 не ставили: блокировка настигла проект ровно в день подготовки выпуска 0.4.0, и владелец, зная о ней, тег не пушил.

**release.sh (E0, прочитан целиком):** локально самодостаточен, CI-шагов не предполагает: зелёный штамп (SHA=HEAD, ярус release, чистое дерево) → `npx tauri build` → build-report.json. Ограничения: производит бандл **только под ОС хоста** (на macOS — только .app/.dmg; это ограничение Tauri, а не скрипта); молчаливая зависимость от python3 (строка 35: без него stamp_value пуст → ложное «штамп относится к другому коммиту» — материал A8); `createUpdaterArtifacts: true` может требовать ключа подписи в окружении локальной сборки (проверяется V-001, исход В).

**make-usb.sh — отсутствующая проверка (E0):** строки ~120–131: `TAG="${VERSION:-$(gh release list … [0].tagName)}"` — берётся последний опубликованный релиз **без сверки с версией рабочего дерева** (tauri.conf.json → 0.4.0). Сегодня это молча даст флешку с v0.3.0. Вторая тихая ветка: `fetch()` при уже существующем файле пишет «есть: имя» и не перекачивает — после подъёма версии движка старые OllamaSetup.exe/архивы сойдут за готовые (риск описан в комментарии make-usb.sh:41-43, но защиты нет). Дополнение внесено в F-PROVISIONING-002; **V-001** (первый номер плана верификации, по требованию заказчика) содержит команды и предсказания для прогона на макбуке сегодня.

## 2. Стратегия ошибок: error.rs + journal.rs (E0, оба файла целиком)

**Что сделано правильно (одной строкой, чтобы не сломали):** типизированные `AppError{code,message}` c пятью кодами и тестом сериализации; `From<String>` честно даёт `unknown`, не угадывая код по тексту; журнал = файл + stderr + хвост 200 записей для экрана «Диагностика», ротация 2 МБ на каждой записи (не при старте), бэклог до-файловых записей сбрасывается в файл, ошибки записи самого журнала глотаются осознанно и задокументированно; `journal::causes()` разворачивает цепочку `source()` с регрессионным тестом ровно на «error decoding response body».

**Систематическая слабость — урок C1-2 применён точечно (E1):**

- `journal::causes()` вызывается ровно **один раз** во всём бэкенде — download.rs:98. Путь загрузки голосовой модели — единственный, где цепочка причин доезжает до журнала.
- `AppError::from_reqwest` (11 мест: chat.rs:184,360; models.rs:109,178,231,337,395; memory.rs:202,225; embed.rs:57) различает timeout/connect по самой ошибке — это хорошо; но ветка «прочее» (error.rs:65) кладёт в message только верхний `Display` (`{e}`) — тот самый зонтик. Класс ошибок «тело оборвалось/не разобралось» на чат-пути снова даст «error decoding response body» без причины — в UI и в журнале.
- **Агентный путь хуже чатового:** agent.rs:127 вообще не использует from_reqwest — голый `format!("Не удалось подключиться к Ollama: {e}")` в String → код unknown; фронт в онлайн-чате не отличит «движок лежит» от прочего, хотя в обычном чате отличает. Несимметрия двух путей одного и того же вызова.
- `AppError` не хранит источник (нет поля/механизма для цепочки) — восстановить причину постфактум из журнала можно только там, где вызвавший сам позвал causes(). → Находка F-BACKEND-004 (P2).

Замечание к полноте кодов: `ErrorCode` не имеет варианта для «нет места на диске» — отказ записи истории/моделей уйдёт как unknown с текстом ОС (по-английски на англ. локали ОС). Вернусь в A2b (файловая система) с конкретными местами.

## 3. cargo-audit: 20 предупреждений — разбор по пункту 3 (E1/E2)

`cargo audit` (E2, эта среда): **0 уязвимостей**, 20 предупреждений. Разделение и достижимость (E1: `cargo tree -i`, включая проверку против `--target x86_64-pc-windows-msvc`):

### Unsound (3) — потенциальная memory-safety, разобраны каждый

| Крейт | Чем тянется | Платформа | Достижимость и вердикт |
|---|---|---|---|
| **anyhow 1.0.102** (RUSTSEC-2026-0190, `Error::downcast_mut`) | tauri 2.11.3 (ядро) + все 5 плагинов + tauri-utils | **все** | В рантайме на каждой платформе. Собственный код jai anyhow не использует (error.rs самописный) — риск ограничен внутренним использованием tauri, а unsound-API узкий (`downcast_mut`). Не балласт, но и не наш код: закроется обновлением tauri-стека. Наблюдать. |
| **event-listener 5.4.1** (`!Send`-теги через StackSlot) | async-broadcast/async-lock ← **zbus** ← tauri-plugin-opener | **только Linux** (zbus в Cargo.toml opener'а под `cfg(linux/bsd)`; в дереве win-msvc отсутствует — E1) | D-Bus-стек открывания ссылок на Linux. На целевой Windows-поставке крейта нет вовсе. Транзитивный балласт чужой платформы для Windows; для Linux-поставки — узкий unsound в редком пути (открытие внешней ссылки). |
| **glib 0.18.5** (`VariantStrIter`) | GTK3-стек: atk/gtk ← muda/tao/webkit2gtk ← tauri | **только Linux** (в дереве win-msvc отсутствует — E1) | GUI-бэкенд Tauri на Linux. Уйдёт вместе с переходом Tauri на GTK4/новый стек; самим проектом не адресуется. |

### Unmaintained (17) — не memory-safety, а прекращение поддержки

- **10× GTK3-биндинги** (atk, gdk, gtk, gdkwayland-sys и др.) — Linux-стек Tauri; судьба привязана к upstream Tauri, проектом не адресуется.
- **6× цепочка pdf-extract** (unic-char-property, unic-char-range, unic-common, unic-ucd-ident, unic-ucd-version, ttf-parser) — **достижимы на всех платформах из основного сценария** (извлечение текста PDF — documents.rs). Это единственная unmaintained-группа, которой стоит заниматься: разбор PDF — поверхность атаки (входной файл от пользователя), а PR #34 уже чинил уязвимости именно в разборе DOCX/ссылок. Наблюдение уже частично есть: `cargo audit` в ярусе `--release`. Кандидат на среднесрочную замену/вендоринг парсера — не сейчас, зафиксировано в реестре (F-BACKEND-005, P3).
- **proc-macro-error** — build-time (макросы), в артефакт не попадает.

## 4. Клубок циклов: поимённо, по пункту 4 (E1: grep + границы #[cfg(test)])

Уточнение к A1: карта модулей A1 считала SCC из 5 модулей **включая тестовые рёбра**. В продакшен-коде download.rs — чистый лист (его ссылки на voice/provision живут в `mod tests`, строки 766–813 > границы 311). Фактическая компонента связности — **4 модуля**.

**Рёбра продакшена, каждое с содержимым:**

| Ребро | Что именно импортируется | Класс связи |
|---|---|---|
| voice → download | `Plan{}`, `resumable()`, `Outcome`, `part_len()`, `RETRY_PAUSES`, `PROGRESS_STEP`, `mb()` (voice.rs:140,252-306) | вызовы + типы (осмысленное: голос пользуется движком загрузки) — вне цикла |
| voice → provision | `hex_digest()` — одна утилита хеширования (voice.rs:202) | вызов; **утилита не про поставку — случайный дом** |
| provision → voice | `MODEL_FILE/MODEL_BYTES/MODEL_SHA256` + `model_path()` (provision.rs:590-696) | константы + функция пути (данные о модели голоса) |
| provision → memory | `GB`, `KV_BYTES_Q8/F16`, `COMPUTE_BUF_GB`, `MEM_SAFETY_GB` + `usable_ram_gb()`, `fit_verdict()` (provision.rs:68,767-784) | константы + вызовы (формула посильности) |
| provision → diagnostics | `detect_vram()` (provision.rs:771) | вызов (детект железа) |
| diagnostics → voice | `model_path()`, `MODEL_BYTES`, `MODEL_SHA256` (diagnostics.rs:58,85-86) | вызов + константы (для build_info) |
| memory → diagnostics | `detect_vram` (memory.rs:10) | вызов |

**Циклы продакшена:** voice ↔ provision (2-цикл); diagnostics → voice → provision → diagnostics (3-цикл); memory → diagnostics → voice → provision → memory (4-цикл).

**Что зациклено: константы и свободные функции. Ни одного разделяемого состояния (статиков/мьютексов) и ни одного типа, пересекающего границу в обе стороны.**

**Ответ на вопрос заказчика («мешают ли тестировать и менять по частям»): сегодня — почти нет, и разрыв дешёвый.**
- Тестировать не мешают: у всех четырёх модулей живые юнит-тесты (voice 6, provision 11, memory 6, diagnostics 4), потому что связи — вызовы чистых функций и константы, не инъекция состояния; заглушек не требуется.
- Менять по частям мешают умеренно: правка формулы в memory или детекта в diagnostics требует перечитать provision (потребителя), но это обычная зависимость, не циклическая ловушка.
- Разрыв, если захочется, — два механических переноса: (1) `hex_digest` → в общие утилиты lib.rs (рвёт voice→provision и с ним все три цикла разом); (2) константы+путь модели распознавания → отдельный модуль данных `voice_model` (рвёт provision→voice и diagnostics→voice). После (1) циклов не остаётся вовсе. Это кандидат в задачи гигиены, не находка-дефект — в реестр не вносится, фиксируется здесь.

## 5. Проглоченные отказы: направленный поиск по пункту 2 (E1 + адверсарная верификация)

Метод: 6 агентов извлечения по группам модулей (все 22), 8 паттернов (`unwrap_or_default`, `unwrap_or(`, `unwrap_or_else`, `.ok()`, `if let Ok` без else, `let _ =`, `Err(_) =>` пусто, `?` в Option-функциях); каждому месту — один вопрос: «при реальном отказе пользователь увидит ошибку или пустоту/успех?»; затем адверсарные верификаторы по каждому кандидату К1/К2 плюс выборочная перепроверка К3. Опровергнуто 9 кандидатов, один повышен К2→К1 — верификация работала в обе стороны.

**Счёт (~336 обследованных мест):**

| Класс | Счёт | Вердикт |
|---|---|---|
| **К1 — ложный успех** | **10** | дефекты, все подтверждены |
| **К2 — ложная пустота** | **24** | дефекты, все подтверждены |
| К3 — тихо, но след в журнале | 6 | приемлемо (Диагностика видит) |
| К4 — законное игнорирование | ~296 | норма: задокументированный best-effort |

**Хорошее — одной строкой:** 296 из ~336 мест — осознанный, прокомментированный best-effort (детект VRAM «честно отдаём None», журнал глотает собственные ошибки записи по замыслу, статусные строки протокола pull) — дисциплина в среднем высокая; дефекты — концентрированные кластеры, а не размазанный стиль.

**Кластеры по последствию (все места — в findings.jsonl):**

| Последствие | Места | Находка |
|---|---|---|
| «Проект удалён со всеми чатами» — а файлы чатов живы (5 точек глотания в каскадном удалении) | projects.rs:145,146,152,153,155 | **F-BACKEND-006 (P1)** |
| Импорт моделей с флешки рапортует успех при пропущенных блобах/манифестах; отказ всплывает позже невнятной ошибкой движка у клиента без интернета; либо ложный диагноз «движок берёт модели из другого каталога» | provision.rs:346-348, 555 (+ нулевые описи носителя: 84, 100, 102) | **F-BACKEND-007 (P1)** |
| settings.json нечитаем (антивирус/повреждение) → ВСЕ настройки молча дефолтные (включая офлайн-пути движка на air-gapped машине), а первое же изменение любой настройки затирает файл дефолтами — безвозвратно и с успехом | settings.rs:28 (+ write_setting:54 — эскалация в потерю) | **F-BACKEND-008 (P1)** |
| Список диалогов: бэкенд сам превращает отказы чтения в пустоту/пропуски (глубже фронтовой F-FRONTEND-002 — чинить обе стороны) | history.rs:73, 83, 96, 97 | **F-BACKEND-009 (P2)** |
| Ошибочный/неожиданный ответ движка = «не установлено»/«Актуально»: статус /api/tags не проверяется в local_digests, формы без поля models дают пустоту; план памяти считает по нулям | models.rs:265, 343, 398 (К1); memory.rs:56, 215, 288 | **F-BACKEND-010 (P2)** |
| Сбой аудиоустройства посреди диктовки неотличим от «человек замолчал»; след — только в stderr, которого у GUI-приложения нет | voice.rs:477 | **F-BACKEND-011 (P2)** |
| Не-JSON строка стрима Ollama молча выбрасывается: обрезанный ответ выглядит нормально завершённым успехом | chat.rs:287 | **F-BACKEND-012 (P2)** |
| Отказ довершения оборванной миграции БД → индекс документов навсегда недостижим, база «пуста» без единого следа | docstore.rs:214 | **F-BACKEND-013 (P2)** |
| Неудачный перезапуск упавшего движка молчит и НАВСЕГДА отключает самовосстановление; в engine.rs нет ни одного вызова журнала | engine.rs:342 | **F-BACKEND-014 (P2)** |
| Мелкие ложные пустоты: «модель голоса не установлена» при недоступном каталоге данных; «0.0 МБ» у базы; пустые результаты веб-поиска при мусорном JSON провайдера | diagnostics.rs:58, 648; tools.rs:902 | **F-BACKEND-015 (P3)** |

**Общий диагноз (продолжение вывода A1):** это не 34 случайных бага, а один непроведённый водораздел — «отказ прочитать/скопировать/удалить ≠ пустота/успех». Проект уже провёл его в лучших местах (delete_project отказывается удалять со ссылкой на «удалил — значит удалено»; load_projects нарочно возвращает Err с человеческим текстом) — кластеры выше суть места, куда правило не дошло. Направление решения одно: короткое правило в CLAUDE.md + точечные правки по списку, начиная с P1.

## Находки этапа (часть 1)

Внесены: **F-BACKEND-006, -007, -008 (P1)**; F-BACKEND-004, -009, -010, -011, -012, -013, -014 (P2); F-BACKEND-005, -015 (P3). Дополнена F-PROVISIONING-002 (таймлайн блокировки, отсутствие сверки версий в make-usb.sh). Клубок циклов — зафиксирован в разделе 4 без находки (не дефект: связность по константам/утилитам, разрыв дешёвый).

## Слепые зоны этого отчёта

| Что не проверено | Почему | ID эксперимента |
|---|---|---|
| Фактическое поведение release.sh/make-usb.sh на макбуке | нет macOS-окружения | V-001 |
| Использование `Error::downcast_mut` внутри tauri (unsound anyhow) | нужен разбор чужого кода за пределами скоупа; принято «наблюдать» | не требуется |
| Воспроизводимость К1-кластеров (залоченный файл на Windows) | нет Windows | V-009 |
| Обрыв миграции docstore + отказ rename (интерливинг) | нужен рантайм с параллельным соединением | V-012 (добавлен в план) |
| Часть 2 скоупа A2 (жизненный цикл, гонки, ФС, async) | вторая сессия A2b | — |

## Скоуп части 2 (сессия A2b, после /clear)

engine.rs: state machine процесса Ollama (started_by_us, порт, гашение группы, две копии, осиротевшие процессы); memory.rs: формула и лестница; download/provision/voice изнутри по результатам классификации части 1; async-дисциплина (блокирующие вызовы в async — кандидаты уже есть: update.rs:104 xdg-open, set_setting файловый I/O; мьютексы через await; задачи без отмены); гонки (двойная отправка, смена модели во время генерации, закрытие окна при загрузке); файловая система (кириллица/пробелы, длина пути Windows, права, полный диск + код ошибки «диск полон» в ErrorCode); ресурсы (буферы, дескрипторы, длинный диалог).
