# Кандидаты в ADR — решения, требующие отдельного обсуждения

Заводится в ходе аудита Фазы 0. Это не находки-дефекты, а решения, которые надо принять явно и записать: каждое меняет правила для всего кода, а не одно место.

---

## ADR-К1 — Правило «отказ ≠ пустота и ≠ успех» + механическая проверка

**Проблема.** Аудит A1/A2a обследовал ~336 мест обработки отказов в Rust и 85 вызовов IPC на фронте. 34 места превращают отказ в ложную пустоту или ложный успех, из них 3 уровня P0 (тихая потеря/неудаление данных клиента) — при том что ~296 мест обработаны корректно. Это не разница в аккуратности, а отсутствие сформулированного правила: одни и те же операции в соседних файлах обработаны противоположно (delete_project отказывается удалять при неудачной чистке со ссылкой на «удалил — значит удалено», projects.rs:125-141; каскадное удаление чатов того же проекта глотает пять отказов подряд, projects.rs:145-155).

**Предлагаемое правило (формулировка для CLAUDE.md):**

> Отказ операции над пользовательскими данными нельзя превращать ни в пустоту, ни в успех.
> 1. **Чтение**: «не удалось прочитать» ≠ «пусто». Отказ read/read_dir/parse — это `Err` с человеческим текстом, а не пустой список и не значение по умолчанию. Исключение — `NotFound`, когда отсутствие данных штатно (первый запуск): его отделять явно.
> 2. **Запись**: не строить новое содержимое файла из данных, прочитать которые не удалось (read-modify-write поверх неудавшегося чтения = уничтожение).
> 3. **Удаление**: «удалил» — значит удалено. Не удалилось хоть что-то — операция отказала, с числом и причиной.
> 4. **Best-effort допустим**, но помечается: `// best-effort: <почему отказ здесь безвреден>`. Без пометки — дефект.
> 5. Любое проглатывание, оставляющее пользователя без информации, обязано хотя бы попасть в журнал (`journal::warn/error`) — иначе поддержка слепа.

**Образцы в коде (на них ссылаться при ревью):** `projects.rs:125-141` (отказ удаления вместо ложного успеха, с обоснованием 152-ФЗ); `projects.rs:42-77` (load_projects возвращает Err с текстом «Проекты НЕ потеряны — файл на месте…» именно для показа человеку); `error.rs:73-77` (From<String> честно ставит код unknown, а не угадывает); `journal.rs:163-166` (единственное осознанное глотание — ошибки записи самого журнала, с объяснением).

**Механическая проверка — спроектирована и посчитана.** Запрет «в лоб» невозможен: в проде 69 `let _ =`, 73 `unwrap_or`, 46 `unwrap_or_else`, 30 `if let Ok`, 22 `.ok()` — законных мест кратно больше дефектных. Проверены четыре признака-разделителя:

| Признак | Годится? | Числа |
|---|---|---|
| **Тип операции** (изменяющие `fs::`) | **да, единственный надёжный** | сужает `let _ =` с 69 до **17** — отсекает 75% шума одним фильтром |
| **Граница `#[cfg(test)]`** | **обязательна** | без неё 17 превращаются в 61 (44 хвоста уборки временных каталогов в тестах) |
| Соседний `journal::` | нет | среди 17 файловых мест рядом нет ни одного вызова журнала: законные не логируют по замыслу, дефектные — по недосмотру. Не разделяет |
| Имя переменной (`tmp`, `part`, `probe`) | ловушка | покрыло бы 10 из 15 законных, но пропустило бы ровно дефектные (`provision.rs:544`, `voice.rs:167`) |

**Предлагаемая проверка** (по образцу `tools/check-dom.py` — python, в ярус `--fast`):

```
для каждого .rs в src-tauri/src:
    отрезать хвост от первого «#[cfg(test)]»
    искать:  let _ = (std::)?fs::(remove_file|remove_dir_all|rename|write|copy)
             let _ = write_atomic
             \.ok\(\) на тех же операциях
    пропускать строки, где на этой или предыдущей строке есть маркер:
             // best-effort: <причина>
    падать со списком «файл:строка + фрагмент» и текстом:
             «Отказ файловой операции проглочен без объяснения.
              Либо вернуть Err, либо пометить: // best-effort: почему это безопасно»
```

**Стоимость и сигнал (посчитано на текущем коде):** 17 срабатываний → **3 настоящих дефекта** (`history.rs:153` очистка истории, `projects.rs:155` каскад удаления, `agent.rs:348` журнал 152-ФЗ — то есть ровно найденные аудитом P0/P1) и **14 мест под маркер** (уборка `.part`/`.copy-part`, пробные файлы, ротация журнала, восстановление БД). Разметка — работа на час; после неё проверка ловит новые случаи с нулевым шумом.

**Чего проверка не ловит** (и это надо принять сознательно): `if let Ok(...)` без `else` — регуляркой не берётся, нужен подсчёт скобок; чтение, превращённое в пустоту (`read_settings`, `list_conversations`) — там нет ключевого паттерна, только структура. Эти случаи остаются на ревью и на юнит-тесты «битый файл → не пусто, а ошибка».

**Альтернатива clippy** оценена и отклоняется как первый шаг: `unwrap_used`/`expect_used` дали бы 173 и 21 сработку (почти всё — в тестах, где они уместны), `let_underscore_must_use` — только на `#[must_use]`-типах, то есть мимо `fs::remove_file`. Узкая своя проверка точнее и дешевле.

**Решение принимает владелец.** Цена: час разметки + правки по списку находок; выгода: класс дефектов, давший 3 из 6 P0 аудита, перестаёт возникать незаметно.

---

## ADR-К2 — Локальный путь производства Windows-сборки

**Проблема.** Установщики под Windows и Linux собирал только GitHub Actions по тегу `app-v*`; после блокировки биллинга (03.08.2026) способа получить `.exe` не осталось вовсе, а Tauri не кросс-компилирует. Это не «CI временно недоступен», а утрата единственного канала производства для приоритетной платформы.

**Что решать:** восстанавливать оплату Actions (быстро, но канал остаётся внешним и повторно ломается) либо завести собственный воспроизводимый Windows-стенд (VM или физическая машина) с задокументированной процедурой. Второе нужно и само по себе: собранный установщик ни разу не запускался на Windows (docs/RELEASE.md), а первый же прогон release.yml в июле упал именно на Windows-специфичном коде.

**Детали, требования и минимальный набор** — `A2-backend.md`, раздел «Локальный Windows-путь»; эксперимент — V-001 в `verification-plan.md`.

---

## ADR-К3 — Разбор недоверенных документов: изоляция парсеров

**Проблема.** Продукт по назначению открывает файлы, приходящие извне (бухгалтерия — PDF и DOCX от контрагентов). Разбор идёт неподдерживаемой цепочкой крейтов (pdf-extract и его unic-*/ttf-parser — 6 предупреждений `unmaintained` в cargo audit): патчей безопасности для неё не будет.

**Что решать:** оставить как есть под наблюдением cargo audit; либо изолировать разбор (отдельный процесс/`catch_unwind` + таймаут + лимит памяти), чтобы враждебный файл не мог уронить или подвесить приложение; либо заменить парсер. Оценка последствий битого/враждебного PDF — `A6-security.md` (раздел «Поверхность атаки: разбор документов»).

---

## ADR-К4 — Разрыв циклической связности пятёрки модулей (гигиена, не дефект)

В проде связаны циклами `voice ↔ provision`, `diagnostics → voice → provision → diagnostics`, `memory → diagnostics → voice → provision → memory`. Зациклены **константы и две свободные функции**, состоянием модули не связаны, тестировать не мешают. Разрыв — два механических переноса: `hex_digest` в общие утилиты (снимает все три цикла) и константы модели распознавания в отдельный модуль данных. Делать не обязательно; решение — «делаем при следующем касании этих файлов» или «оставляем».

---

## ADR-К5 — Набор для измерения качества поиска по документам

**Проблема.** Качество RAG сегодня не измеряется ничем (F-RAG-008, P0). Единственные два сквозных теста помечены `#[ignore]` и не запускаются ни одним ярусом гейта; проверка качества в них — `top.contains("50000")` на трёх однопредложенных «документах», то есть тест останется зелёным при смене `k`, порога, размера чанка и при заметной деградации модели. Эталонного корпуса, метрик и логирования расстояний нет. Следствие: **любая правка пайплайна делается вслепую**, и это блокирует волну улучшения RAG целиком — включая исправление уже найденных дефектов (F-RAG-002 «top-6 → 2–3 фрагмента», F-RAG-003 «порог 0.6 отсекает молча»), потому что менять порог, не умея измерить результат, бессмысленно.

**Решение к принятию.** Завести офлайновый набор регрессии качества, живущий в репозитории и прогоняемый одной командой.

**Состав** (подобран под фактическую аудиторию — бухгалтерия, юридические практики, клиники):

| Часть | Что именно | Почему так |
|---|---|---|
| Корпус | 6–8 обезличенных документов: договор оказания услуг, счёт-фактура, акт, учётная политика, штатное расписание, инструкция. Форматы PDF и DOCX. **Обязательно один скан без текстового слоя и один с таблицей** | На сканах и таблицах извлечение ломается (F-RAG-006); без них набор не покроет главные слабые места |
| Вопросы | 25–30, по типам: фактические, по таблице, по формулировке, с синонимией («НДС» / «налог на добавленную стоимость»), с числом/датой/кодом, и **3–5 вопросов не по документам** | Типы ломаются по-разному; вопросы «мимо» проверяют, что система честно говорит «не нашлось», а не выдумывает |
| Эталон | Для каждого вопроса: id документа + номера фрагментов с ответом + короткая цитата. Один JSON рядом с корпусом | Номера — для автоматики, цитата — для человека при разборе регрессии |
| Метрики | recall@k (главная), MRR, доля вопросов без единого фрагмента после порога, **распределение `distance` по типам вопросов** | Последняя метрика — то, чем откалибровать порог 0.6 фактами вместо круглого числа |
| Прогон | `cargo test --ignored rag_quality` (тот же механизм, что у существующих live-тестов) либо `tools/rag-eval.sh`; печатает таблицу и сравнение с базовой линией | Требует локальной Ollama с bge-m3 — как и нынешние `#[ignore]`-тесты |
| Регрессия | Базовая линия в репозитории; падение recall@k более чем на 5 п.п. — красный | Даёт объективный критерий «стало хуже» |

**Важная деталь исполнения:** ярус `--release` сегодня Ollama **не требует** (слов `ollama` в `check.sh` и `smoke.sh` нет вовсе), поэтому просто «добавить `--ignored` в существующий ярус» недостаточно. Нужен либо отдельный ярус с явной проверкой наличия движка, либо использование механизма `skip()` (`check.sh:66`), который при пропуске проверки отменяет зелёный штамп — тогда «качество не измерялось» будет честно блокировать выпуск, а не молча пропускаться.

**Цена: 3–4 дня** — день на подбор и обезличивание документов, полтора на разметку эталона, день на прогонщик и метрики, полдня на базовую линию.

**Что решает владелец:** делать ли набор до правок RAG (рекомендация аудита — да, иначе правки непроверяемы) и кто размечает эталон (это предметная работа: нужно знать, где в договоре лежит правильный ответ).

---

## ADR-К6 — Размещение данных: Roaming, каталог, портативный режим

**Проблема.** На Windows каталог данных лежит в `%APPDATA%` (Roaming): в домене база документов, переписка и модель распознавания (465 МиБ) синхронизируются на файловый сервер при каждом входе и выходе (F-SECURITY-006, P0). При перенаправленных папках база SQLite в режиме WAL оказывается поверх SMB — путь к молчаливому повреждению (F-PROVISIONING-004).

**Что решать:** (1) переносить ли данные в `%LOCALAPPDATA%` — по сути да, вопрос лишь в том, делать ли каталог настраиваемым; (2) выносить ли модель распознавания за пределы каталога пользовательских данных (это неизменяемый ресурс, ему в профиле не место); (3) делать ли из настраиваемого каталога портативный режим — «портативные данные, системный движок» (полностью портативным продукт быть не может: движок и WebView2 системные).

**Рекомендация: принять до волны 0** в части (1) и (2) — это дешёвые правки путей, а установок ноль, миграция не нужна. Портативный режим (3) — решение отдельное, +1–2 дня поверх, можно позже.

**Детали:** `A7-provisioning.md`, раздел 3.

---

## ADR-К7 — Автонастройка: измерение вместо предсказания

**Проблема.** Механизмы адаптации к слабому железу увеличивают работу именно на слабом железе — три подтверждённых случая (F-PRODUCT-001), причём один такой же случай автор уже находил и исправлял точечно. Формула памяти чувствительнее к погрешности замера, чем ширина её собственной зоны неопределённости (F-ENGINE-004). При этом движок присылает фактическую скорость и цену загрузки в каждом ответе, а код их выбрасывает (F-ENGINE-007).

**Что решать:** переходить ли от предсказания к измерению — классы по паре (модель, контекст) на двух осях, зона «впритык» шириной в измеренную σ, рычаги отсортированные по цене, правило «5% экономии на гигабайт добавленного времени».

**Рекомендация: принять до волны 0 в части первых четырёх шагов** (чтение done-кадра, порог класса в гибибайтах, `keep_alive`, отключение `num_thread` для CPU-класса) — вместе около двух дней, и они дают основную часть выигрыша. Полная перестройка классов — позже.

**Детали:** `design-autoconfig.md` (отдельный документ).

---

## ADR-К8 — Подпись поставки с носителя

**Проблема.** Обновление с флешки устанавливается без проверки подписи, хотя это основной канал для офлайн-клиента (F-SECURITY-008, P0). При этом подписи артефактов **уже существуют** — `tauri-action` подписывает всё, а `make-usb.sh` их выбрасывает (F-PROVISIONING-008).

**Что решать:** двухъярусную схему — скрипт установки сверяет sha256 по манифесту (`certutil`/`Get-FileHash` есть на любой Windows), приложение при первом запуске проверяет Ed25519-подпись манифеста через `minisign-verify` (крейт уже в дереве). Плюс: где хранится приватный ключ и как его резервируют (F-SECURITY-003).

**Рекомендация: принять до волны 0.** Вопрос ключа — вообще до всего остального: пока он существует только в секретах заблокированного аккаунта, любая работа над каналом поставки условна.

**Детали:** `A7-provisioning.md`, раздел 2; `A8-quality.md`, раздел 3.

---

## Сводка: что решать до волны 0, что можно позже

| ADR | Суть решения | Стоимость | Когда |
|---|---|---|---|
| **К8** | где живёт ключ подписи + подпись поставки с носителя | 0.5 дня на ключ, 1–2 на подпись | **до всего**: без ключа канал поставки условен |
| **К2** | Windows-стенд: оплатить Actions или поднять свой | 1–2 дня | **до волны 0**: без него нет ни выпуска, ни приёмки |
| **К1** | правило «отказ ≠ пустота» + механическая проверка | 1 час разметки + правки по списку | **до волны 0**: закрывает 7 из 14 P0 |
| **К6** | данные из Roaming в Local, модель STT за пределы | S | **до волны 0**: правка путей, миграция не нужна |
| **К7** (часть) | чтение done-кадра, порог класса, `keep_alive`, `num_thread` | ~2 дня | **до волны 0**: даёт основную часть выигрыша на целевой машине |
| **К5** | набор для измерения качества RAG | 3–4 дня | **до правок RAG**, иначе они непроверяемы |
| **К3** | изоляция разбора недоверенных документов | M | позже: сегодня закрыто `catch_unwind`, остаются два вектора |
| **К7** (полностью) | перестройка классов на две оси | M | позже, после первых четырёх шагов |
| **К6** (портативный режим) | «портативные данные, системный движок» | +1–2 дня | позже, по спросу |
| **К4** | разрыв циклической связности модулей | S | **никогда специально**: делать при следующем касании этих файлов |

Единственное решение, которое стоит принять **сегодня, до планирования волны**, — К8 в части ключа: это не работа, а ответ на вопрос «существует ли он вне GitHub», и от ответа зависит, есть ли у продукта канал доставки исправлений вообще.
