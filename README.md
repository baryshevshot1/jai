# jai

Боксовый офлайн ИИ-ассистент для бизнеса. Десктоп-приложение, которое после установки работает **полностью на компьютере пользователя**: без облака, данные не покидают ПК (152-ФЗ), без терминала.

## Стек

- **Tauri 2** (Rust + TypeScript/Vite)
- **Ollama** — единственный движок нейросетей, локально (`127.0.0.1:11434`)
- **sqlite-vec + bge-m3** — поиск по документам (RAG)

## Возможности (MVP)

- Чат с локальной моделью (текст, русский профиль)
- Поиск по своим документам (PDF/Word)
- Зрение и распознавание текста/таблиц (OCR)
- Навык кода
- Подбор моделей под железо (светофор), офлайн-работа после установки

## Разработка

Только на macOS:

```bash
npm install
npm run tauri dev
```

Откроется окно приложения; Vite поднимается на `http://localhost:1420`.

## Сборка установщиков

Под Linux и Windows собираются в **GitHub Actions** (Tauri не умеет кросс-компиляцию) по пушу тега:

```bash
git tag app-vX.Y.Z
git push --tags
```

Готовые установщики появляются в разделе **Releases** (черновик релиза).

## Структура

```
src/              — фронтенд (TypeScript)
src-tauri/        — бэкенд Rust + конфиг Tauri
.github/workflows — CI-сборка
docs/             — руководство и инженерный отчёт
CLAUDE.md         — контекст проекта для Claude Code
```

## Документация

Полное руководство по разработке и инженерный отчёт — в папке [`docs/`](./docs).

---

© 2026 Андрей (baryshevshot1). Проприетарный проект — см. [LICENSE](./LICENSE).
